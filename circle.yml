machine:
  services:
    - docker

dependencies:
  pre:
  	- sudo pip install docker-compose
  override:
    - docker-compose run web npm install

test:
  override:
    - docker-compose run web npm run test:server
